[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Enable proper routing for single-page apps (React Router / Vite)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
